# Production Chat App Configuration
spring.application.name=chat-app

# Neon PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://ep-icy-feather-ahm1iuti-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_wXsdknKT2cE5

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwt.secret=${JWT_SECRET:very_secret_complex_key_at_least_32_chars_long}
app.jwt.expiration-ms=3600000

# Redis Configuration (For Presence & Pub/Sub)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# Server Settings
server.port=8080
server.error.include-message=always

# Email Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mugishaivanbright250@gmail.com
spring.mail.password=wtlv syvg ctem fvtm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
